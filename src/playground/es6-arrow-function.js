//es5
const square = function(x) {
  return x * x;
};

//es6
//her zaman anonimdir. onun için değişkene atamak şarttır.
const squareArrow = x => {
  return x * x;
};

const squareArrow1 = x => x * x;
